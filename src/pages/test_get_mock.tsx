{/*import useStatus from '../stores/Reservation_status';
import mock from "../services/Mocks/Reservation_mock";
function Test() {
    const { fetchStatus, status, amount,done_count,cancelled_count,rejected_count,reserved_count,pending_count } = useStatus();
    return(
        <div>
        <h1>testing</h1>
        <button className="bg-blue-500 text-white px-4 py-2 rounded" onClick={() => fetchStatus("123")}>Fetch Status</button>
        <p>amount: {amount}</p>
        <p>amount of done: {done_count}</p>
        <p>amount of pending: {pending_count}</p>
        <p>amount of cancelled: {cancelled_count}</p>
        <p>amount of rejected: {rejected_count}</p>
        <p>amount of reserved: {reserved_count}</p>
        <p>Status: {status}</p>
        </div>
    )
}

export default Test;*/}